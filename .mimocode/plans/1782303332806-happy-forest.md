# Plano: Limpeza completa do Codex VSCode

## Diagnóstico

### Erros encontrados nos logs

1. **`ngs-analysis` plugin prompt > 128 chars** (CORRIGIDO na rodada anterior)
   - O arquivo `plugin.json` já foi corrigido para 109 chars
   - Porém o repo git em `.tmp/plugins/` está "dirty" (modificação não commitada)
   - O Codex pode re-clonar o repo do zero ao iniciar, ignorando mudanças locais

2. **`[IpcClient] Received broadcast but no handler is configured method=client-status-changed`**
   - Warning benigno do VSCode IPC - extensão recebe broadcast mas não tem handler registrado
   - NÃO é erro fatal - extensão continua funcionando após isso
   - Provável mismatch entre versão do VSCode extension (26.5616.81150) e o CLI binary

3. **`codex_models_manager::manager: failed to refresh available models: timeout waiting for child process to exit`**
   - Timeout ao buscar lista de modelos do chatgpt.com
   - Pode ser causado por conectividade ou pelo banco de logs grande

### Dados para limpar

| Item | Tamanho | Qtd |
|------|---------|-----|
| logs_2.sqlite | 36MB | 20.434 entries (16K TRACE) |
| archived_sessions | ~37 arquivos rollout | sessões antigas |
| session_index.jsonl | 34 sessões | incluindo threads antigas |
| .bak files | 1 arquivo | backup do plugin.json |
| .tmp-backup-20260623-ipc/ | diretório | backup IPC obsoleto |
| git dirty state | repo plugins | modificação não commitada |

---

## Plano de execução

### Passo 1: Restaurar plugins repo ao estado limpo
**Arquivo:** `/home/tiagolocal/.codex/.tmp/plugins/plugins/ngs-analysis/.codex-plugin/plugin.json`

- Restaurar o plugin.json ao valor original (revert our change)
- Remover o arquivo `.bak`
- Rodar `git checkout -- .` no repo de plugins para limpar o dirty state
- **Razão:** O Codex re-sincroniza plugins do git remote. Modificações locais são sobrescritas. O fix do prompt precisa ser feito de outra forma (passo 2)

### Passo 2: Desabilitar o plugin ngs-analysis via config
**Arquivo:** `/home/tiagolocal/.codex/config.toml`

- Adicionar `[plugins."ngs-analysis"]` com `enabled = false`
- Isso impede que o Codex carregue o plugin com prompt longo
- Não depende de modificações no git repo

### Passo 3: Limpar banco de logs
**Arquivo:** `/home/tiagolocal/.codex/logs_2.sqlite`

- Rodar `VACUUM` para compactar (36MB → ~2MB com 20K entries truncadas)
- Ou删除 entries TRACE/DEBUG antigas e VACUUM

### Passo 4: Limpar sessões antigas (manter apenas hoje 24/jun)
**Diretórios:** 
- `/home/tiagolocal/.codex/archived_sessions/` - remover TODOS os rollout files (37 arquivos)
- `/home/tiagolocal/.codex/session_index.jsonl` - reescrever mantendo apenas sessões de 2026-06-24
- `/home/tiagolocal/.codex/sessions/2026/` - limpar subdiretórios 02/ a 06/ (manter apenas 06/ para junho, e dentro de 06/ apenas dia 24)

### Passo 5: Limpar backups e temporários
- Remover `/home/tiagolocal/.codex/.tmp-backup-20260623-ipc/`
- Remover `/home/tiagolocal/.codex/plugins/ngs-analysis/.codex-plugin/plugin.json.bak`

### Passo 6: Limpar regras de sessões anteriores
**Arquivo:** `/home/tiagolocal/.codex/rules/default.rules`

- Manter apenas regras essenciais (rg, npm run dev, curl)
- Remover regras de manipulação de extension.js e mcp.json que são obsoletas

---

## Verificação

1. Fechar VSCode completamente
2. Reabrir VSCode
3. Verificar Output > Codex para confirmar:
   - Sem warning de `defaultPrompt` > 128 chars
   - `client-status-changed` pode continuar (é benigno)
   - Sem erros de timeout de modelos
4. Testar uma conversa simples no Codex